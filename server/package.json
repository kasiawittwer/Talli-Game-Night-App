{
  "name": "my-game-assistant-api",
  "version": "1.0.0",
  "private": true,
  "description": "Optional proxy so OpenAI keys stay off the device (same pattern as chatbot-website + Anthropic)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
