/**
 * iPhone 17 Pro logical viewport (CSS px) — matches Safari/Chrome layout width/height at 1x.
 * Physical resolution uses @3x; layout code should use these values.
 */
export const IPHONE_17_PRO_VIEWPORT = {
  width: 402,
  height: 874,
} as const;
